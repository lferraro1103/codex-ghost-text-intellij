---
type: quick
status: planned
---

# Repair phase 1 audit coverage and verification tracking

1. Extend action tests to exercise keyboard-place dispatch, observe actual native notifications, missing context, direct menu registration and no default shortcuts. Extend resolver tests for exact UTF-16 ranges, whitespace, EOF, stale PSI and malformed offsets. Fix only defects reproduced by these tests.
2. Run fresh tests and buildPlugin using the wrapper (force test execution, retain output and timestamps). Run compatibility verification for changed production APIs if needed.
3. Create accurate phase UAT/verification artifacts, keeping the mandatory two-version human checkpoint pending. Update STATE and record the quick-task summary. Do not claim Phase 1 complete or manufacture human approval. Then assess Phase 2 entry against the existing blocking checkpoint.

Verification: actual notification capture and source immutability assertions; fresh test XML with zero failures; audit-uat must discover outstanding manual verification instead of reporting zero files.

Execution is inline under the skill's Codex fallback; no independent subtask warrants delegation.
